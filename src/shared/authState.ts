// Временная заглушка авторизации: в проекте пока нет модуля auth (src/app
// и src/shared были пустыми заготовками -- grep по "auth"/"isLoggedIn"
// ничего не находил). Это единая точка правды для всех мест, что зависят
// от состояния логина -- шапка (desktop-блок действий и tablet-кнопка) и
// мобильное меню -- чтобы флаг не разъезжался по нескольким локальным
// копиям. Имя и инициалы -- как в присланном референсе навбара
// (John Doe / JD), тоже заглушка на месте будущего реального юзера.
// Когда появится настоящая авторизация, отсюда же экспортировать
// актуальное состояние вместо константы.
export const CURRENT_USER = {
  isLoggedIn: false,
  name: 'John Doe',
  initials: 'JD',
} as const;
